{
  "name": "pta-pilot",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "pnpm --filter @pta-pilot/shared build && concurrently -n shared,api,web -c yellow,cyan,magenta \"pnpm --filter @pta-pilot/shared dev\" \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "turbo": "^2.5.0"
  }
}
